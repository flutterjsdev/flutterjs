/**
 * MainAxisSize - How much space should be occupied along the main axis
 */
const MainAxisSize = Object.freeze({
  // As much space as possible
  max: 'max',

  // As little space as possible
  min: 'min'
});
export { MainAxisSize };