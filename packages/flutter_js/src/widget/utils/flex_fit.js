/**
 * FlexFit - How children fill the available space
 * Used with Flexible widget
 */
const FlexFit = Object.freeze({
  // Child is allowed to be smaller than the flex space
  loose: 'loose',

  // Child is forced to fill the flex space
  tight: 'tight'
});
export { FlexFit };