/**
 * AlignmentDirectional - Directional alignment using start/end instead of left/right
 * Respects text direction (LTR/RTL)
 */
const AlignmentDirectional = Object.freeze({
  topStart: 'topStart',
  topCenter: 'topCenter',
  topEnd: 'topEnd',
  centerStart: 'centerStart',
  center: 'center',
  centerEnd: 'centerEnd',
  bottomStart: 'bottomStart',
  bottomCenter: 'bottomCenter',
  bottomEnd: 'bottomEnd'
});

export { AlignmentDirectional };
